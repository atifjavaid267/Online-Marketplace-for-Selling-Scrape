<%= render 'users/navbar' %>


<div class="py-8">
  <% if @addresses.empty? %>
    <h4 class="text-center text-gray-600 mb-4">No address yet!!!</h4>
  <% else %>
    <table class="w-full table-auto">
      <thead>
        <tr class="bg-gray-200 text-gray-800 text-left font-bold">
          <th class="px-4 py-2">No.</th>
          <th class="px-4 py-2">Street 1</th>
          <th class="px-4 py-2">Street 2</th>
          <th class="px-4 py-2">City</th>
          <th class="px-4 py-2">State</th>
          <th class="px-4 py-2">Zip Code</th>
          <th class="px-4 py-2">Latitude</th>
          <th class="px-4 py-2">Longitude</th>
          <th class="px-4 py-2" colspan="2">Actions</th>
        </tr>
      </thead>
      <tbody>
        <% counter = 1 %>
        <% @addresses.each do |address| %>
          <tr class="<%= cycle('bg-white', 'bg-gray-100') %>">
            <td class="px-4 py-2"><%="#{counter}."%></td>
            <td class="px-4 py-2"><%= address.street1 %></td>
            <td class="px-4 py-2"><%= address.street2 %></td>
            <td class="px-4 py-2"><%= address.city %></td>
            <td class="px-4 py-2"><%= address.state %></td>
            <td class="px-4 py-2"><%= address.zip_code %></td>
            <td class="px-4 py-2"><%= address.latitude %></td>
            <td class="px-4 py-2"><%= address.longitude %></td>
            <td class="px-4 py-2"><%= link_to "edit", edit_address_path(address), class: "bg-green-500 hover:bg-green-700 text-white py-2 px-3 rounded" %></td>
            <td class="px-4 py-2"><%= button_to "delete", address, method: :delete, class: "bg-red-500 hover:bg-red-700 text-white  py-2 px-3 rounded" %></td>
          </tr>
          <% counter += 1 %>
        <% end %>
      </tbody>
    </table>
  <% end %>
</div>

<div class="flex justify-between items-center py-8">
  <%= link_to "Add Address", new_address_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full focus:outline-none focus:shadow-outline" %>
</div>

<button class="bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold py-2 px-4 rounded" style="margin-left: 10px;" onclick="window.history.back();">Back</button>
